# Extension — Chrome (Phase 2)

Placeholder. Not part of the MVP.

Phase 2 (stretch goal): reuse the form-mapping engine (FormMappingService / DraftGenerationService) inside a Chrome MV3 extension for third-party career portals, calling the same `/assistant/*` API contract. See development document v0.2 §9.4 for the deferred complexity analysis (DOM, MV3 lifecycle, content script, cross-origin iframes, dynamic forms, permissions).
