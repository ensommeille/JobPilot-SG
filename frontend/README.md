# Frontend — React + TypeScript + Vite

Placeholder for the Phase 1 UI shell (M1: TANG YUCHEN).

Scaffold with Vite when Sprint 1 starts:

```bash
npm create vite@latest frontend -- --template react-ts
```

Planned screens: login, job list/filters, job detail, favorites, profile, application form + AI confirmation panel.
